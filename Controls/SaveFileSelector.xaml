<Control x:Class="ColdShineSoft.PostEmulator.Controls.SaveFileSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
	<Control.Template>
		<ControlTemplate>
			<DockPanel VerticalAlignment="Top" DataContext="{Binding RelativeSource={RelativeSource Mode=TemplatedParent}}">
				<Button Click="Button_Click" DockPanel.Dock="Right" Margin="5,0,0,0" Padding="10,0,10,0" Content="{Binding OpenButtonText}" FontSize="{Binding FontSize}" />
				<TextBox Text="{Binding Path=Path,UpdateSourceTrigger=LostFocus}" />
			</DockPanel>
		</ControlTemplate>
	</Control.Template>
</Control>